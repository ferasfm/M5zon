-- ========================================
-- إضافة المستخدم admin (نسخة مبسطة)
-- ========================================

-- الخطوة 1: حذف المستخدم admin إذا كان موجوداً
-- ========================================
DELETE FROM users WHERE username = 'admin';

-- الخطوة 2: إضافة المستخدم admin
-- ========================================
-- هذا يعمل مع الأعمدة الأساسية فقط

-- ⚠️ أولاً: شغّل هذا الأمر لتوليد كلمة المرور المشفرة:
-- node generate-password-hash.js

-- ثم استبدل REPLACE_WITH_HASHED_PASSWORD بالنتيجة

INSERT INTO users (username, password_hash)
VALUES ('admin', '$2a$10$REPLACE_WITH_HASHED_PASSWORD');

-- إذا كان الجدول يحتوي على أعمدة إضافية، استخدم هذا:
-- INSERT INTO users (username, password_hash, role, is_active)
-- VALUES ('admin', '$2a$10$REPLACE_WITH_HASHED_PASSWORD', 'admin', true);

-- ========================================
-- الخطوة 3: التحقق
-- ========================================
SELECT * FROM users WHERE username = 'admin';

-- ========================================
-- ✅ اكتمل!
-- ========================================
-- 
-- بيانات الدخول:
-- اسم المستخدم: admin
-- كلمة المرور: admin123
-- ========================================
