# 🔑 إصلاح كلمة المرور

## المشكلة:
كلمة المرور المشفرة في قاعدة البيانات غير صحيحة.

---

## ✅ الحل (خطوة واحدة):

### **في pgAdmin → Query Tool، شغّل:**

```sql
UPDATE users 
SET password_hash = '$2b$10$GOsEh/JO08xgRzyi8m69x.0HRWnJPE3B39oU/jG7/uEOOqrSUUcy2'
WHERE username = 'admin';
```

**اضغط F5 أو زر ▶️**

---

## 🚀 بعد ذلك:

### **1. شغّل التطبيق:**
```bash
dist\m5zon 1.0.0.exe
```

### **2. سجل الدخول:**
```
اسم المستخدم: admin
كلمة المرور: admin123
```

---

## ✅ يجب أن يعمل الآن!

إذا لم يعمل، تأكد من:
- ✅ تم تنفيذ الأمر بنجاح في pgAdmin
- ✅ لا توجد مسافات إضافية في كلمة المرور المشفرة
- ✅ المستخدم admin موجود في قاعدة البيانات

---

## 🔍 للتحقق:

```sql
SELECT username, password_hash 
FROM users 
WHERE username = 'admin';
```

يجب أن ترى:
- username: `admin`
- password_hash: `$2b$10$GOsEh/JO08xgRzyi8m69x...`

---

**جرب الآن! 🎉**
