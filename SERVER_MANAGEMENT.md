# دليل إدارة الخادم
## Server Management Guide

هذا الدليل يشرح كيفية استخدام نظام إدارة الخادم المحسن لمنع التشغيل المتعدد وإدارة البرنامج بكفاءة.

## 🚀 الأوامر المتاحة

### أوامر التشغيل الأساسية

```bash
# تشغيل البرنامج (مع فحص التشغيل المتعدد)
npm run dev
# أو
npm start

# تشغيل قسري (بدون فحص)
npm run dev:force

# تشغيل آمن (مع فحص شامل)
npm run dev:safe
```

### أوامر المراقبة والإدارة

```bash
# فحص حالة البرنامج
npm run status

# فحص توفر المنفذ
npm run check-port

# فحص العمليات الجارية
npm run check-running

# إيقاف البرنامج
npm run stop

# إعادة تشغيل البرنامج
npm run restart
```

### أوامر التكوين المتقدمة

```bash
# إدارة الخادم مباشرة
npm run server:config start
npm run server:config stop
npm run server:config status
npm run server:config check
```

## ⚙️ إعدادات التكوين

يمكنك تخصيص إعدادات الخادم في ملف `server.config.js`:

### إعدادات المنفذ
```javascript
port: 3000,           // رقم المنفذ
host: 'localhost',    // عنوان المضيف
```

### إعدادات الأمان
```javascript
security: {
  preventMultipleInstances: true,  // منع التشغيل المتعدد
  autoKillExisting: false,         // إيقاف العمليات الموجودة تلقائياً
  maxRetries: 3,                   // عدد المحاولات القصوى
  retryDelay: 2000,               // تأخير بين المحاولات (بالميلي ثانية)
}
```

### إعدادات التشغيل
```javascript
startup: {
  openBrowser: false,        // فتح المتصفح تلقائياً
  showLogs: true,           // عرض السجلات
  clearConsole: true,       // مسح وحدة التحكم عند البدء
  showWelcomeMessage: true, // عرض رسالة الترحيب
}
```

### إعدادات المراقبة
```javascript
monitoring: {
  healthCheck: true,           // فحص صحة الخادم
  healthCheckInterval: 30000,  // فترة فحص الصحة (30 ثانية)
  logActivity: true,           // تسجيل الأنشطة
  saveProcessInfo: true,       // حفظ معلومات العملية
}
```

## 📁 الملفات المُنشأة

### ملفات القفل والمراقبة
- `.server.lock` - ملف قفل يحتوي على معلومات العملية الحالية
- `.server.pid` - ملف يحتوي على معرف العملية
- `logs/server.log` - سجل أنشطة الخادم

### ملفات السكريبتات
- `scripts/start-server.js` - سكريبت بدء الخادم المحسن
- `scripts/check-port.js` - فحص توفر المنفذ
- `scripts/check-running.js` - فحص العمليات الجارية
- `scripts/stop-server.js` - إيقاف الخادم
- `server.config.js` - ملف التكوين الرئيسي

## 🔧 استكشاف الأخطاء

### المشكلة: "المنفذ مستخدم بالفعل"

**الحل:**
```bash
# فحص العمليات الجارية
npm run status

# إيقاف العمليات الموجودة
npm run stop

# إعادة المحاولة
npm run dev
```

### المشكلة: "لا يمكن إيقاف العملية"

**الحل اليدوي:**
```bash
# في Windows
netstat -ano | findstr :3000
taskkill /PID [رقم_العملية] /F

# في Linux/Mac
lsof -ti :3000
kill -9 [رقم_العملية]
```

### المشكلة: "ملف القفل عالق"

**الحل:**
```bash
# حذف ملفات القفل يدوياً
rm .server.lock .server.pid

# أو استخدام سكريبت التنظيف
node server.config.js stop
```

## 🎯 المميزات الرئيسية

### ✅ منع التشغيل المتعدد
- فحص تلقائي للمنفذ قبل التشغيل
- رسائل واضحة عند وجود تضارب
- خيارات متعددة للتعامل مع التضارب

### ✅ مراقبة متقدمة
- فحص صحة الخادم بشكل دوري
- تسجيل جميع الأنشطة
- معلومات تفصيلية عن حالة النظام

### ✅ إدارة محسنة
- إيقاف آمن مع تنظيف الملفات
- معالجة الأخطاء غير المتوقعة
- دعم أنظمة تشغيل متعددة

### ✅ واجهة سهلة
- أوامر بسيطة ومفهومة
- رسائل باللغة العربية
- معلومات مفيدة وواضحة

## 🔄 سير العمل المُوصى به

### للتطوير اليومي:
```bash
# بدء العمل
npm run dev

# فحص الحالة عند الحاجة
npm run status

# إيقاف عند الانتهاء
npm run stop
```

### لاستكشاف المشاكل:
```bash
# فحص شامل
npm run status
npm run check-running

# إيقاف قسري إذا لزم الأمر
npm run stop

# إعادة تشغيل نظيف
npm run restart
```

### للنشر والإنتاج:
```bash
# بناء المشروع
npm run build

# معاينة النسخة المبنية
npm run preview
```

## 📞 الدعم

إذا واجهت أي مشاكل:

1. **تحقق من السجلات**: `logs/server.log`
2. **فحص الحالة**: `npm run status`
3. **إعادة تشغيل نظيف**: `npm run restart`
4. **تنظيف يدوي**: حذف `.server.lock` و `.server.pid`

---

**ملاحظة**: هذا النظام مصمم لبيئة التطوير. للإنتاج، يُنصح باستخدام مدير عمليات مثل PM2 أو Docker.