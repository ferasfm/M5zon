# ملفات التشغيل التنفيذية (.bat)

## 🚀 الملفات المتاحة:

### 📦 `install.bat`
**الاستخدام الأول** - تثبيت التبعيات
- يفحص وجود Node.js
- يثبت جميع التبعيات المطلوبة
- **شغله أولاً قبل أي شيء آخر**

### 🚀 `start.bat`
**تشغيل البرنامج**
- يفحص المنفذ 3000
- يمنع التشغيل المتعدد
- يعرض رسائل واضحة
- يفتح البرنامج على http://localhost:3000

### 🛑 `stop.bat`
**إيقاف البرنامج**
- يوقف جميع العمليات على المنفذ 3000
- ينظف النظام
- يعرض رسائل تأكيد

### 📊 `status.bat`
**فحص حالة البرنامج**
- يعرض حالة النظام
- يفحص Node.js و npm
- يعرض العمليات النشطة
- يفحص ملفات المشروع

### 🔄 `restart.bat`
**إعادة تشغيل البرنامج**
- يوقف البرنامج الحالي
- ينتظر قليلاً
- يشغل البرنامج مرة أخرى

## 📋 ترتيب الاستخدام:

### المرة الأولى:
1. **انقر مرتين على `install.bat`** - لتثبيت التبعيات
2. **انقر مرتين على `start.bat`** - لتشغيل البرنامج

### الاستخدام اليومي:
- **`start.bat`** - لتشغيل البرنامج
- **`stop.bat`** - لإيقاف البرنامج
- **`status.bat`** - لفحص الحالة

### عند المشاكل:
- **`restart.bat`** - لإعادة تشغيل نظيف
- **`stop.bat`** ثم **`start.bat`** - للتحكم اليدوي

## 🎯 مميزات الملفات التنفيذية:

✅ **سهولة الاستخدام** - نقرة مزدوجة فقط
✅ **رسائل باللغة العربية** - واضحة ومفهومة  
✅ **فحص تلقائي** - للمتطلبات والمشاكل
✅ **منع التشغيل المتعدد** - حماية من التضارب
✅ **معالجة الأخطاء** - رسائل مفيدة عند المشاكل
✅ **تنظيف تلقائي** - إدارة العمليات والذاكرة

## 🔧 استكشاف الأخطاء:

### "Node.js غير مثبت"
- حمل وثبت Node.js من: https://nodejs.org
- اختر النسخة LTS (الموصى بها)

### "المنفذ 3000 مستخدم"
- استخدم `stop.bat` لإيقاف البرنامج الحالي
- أو افتح http://localhost:3000 إذا كان يعمل

### "فشل في تثبيت التبعيات"
- تأكد من اتصال الإنترنت
- شغل Command Prompt كمدير
- احذف مجلد `node_modules` وأعد تشغيل `install.bat`

## 💡 نصائح:

- **احتفظ بالملفات في نفس مجلد المشروع**
- **شغل `install.bat` مرة واحدة فقط عند التثبيت**
- **استخدم `status.bat` لفحص المشاكل**
- **`restart.bat` يحل معظم المشاكل**

---

**الآن يمكنك تشغيل البرنامج بسهولة! انقر مرتين على `start.bat` 🚀**