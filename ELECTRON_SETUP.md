# 🚀 تطبيق Electron - نظام إدارة المخزون الاحترافي

## نظرة عامة
تم تحويل التطبيق إلى تطبيق سطح مكتب باستخدام **Electron**. يمكنك الآن تشغيل التطبيق كتطبيق مستقل على سطح المكتب بدلاً من المتصفح.

## المتطلبات
- **Node.js** (الإصدار 14 أو أحدث)
- **npm** (يأتي مع Node.js)

## التثبيت الأولي

### الخطوة 1: تثبيت التبعيات
```bash
npm install
```

## طرق التشغيل

### 1️⃣ تشغيل التطبيق في وضع التطوير
**استخدم الملف:** `start-electron.bat`

أو من سطر الأوامر:
```bash
npm run dev:electron
```

هذا سيقوم بـ:
- بدء خادم Vite على المنفذ 5173
- فتح نافذة Electron تلقائياً
- تفعيل أدوات المطور (DevTools)

### 2️⃣ بناء التطبيق للإنتاج
**استخدم الملف:** `build-electron.bat`

أو من سطر الأوامر:
```bash
npm run build:electron
```

هذا سيقوم بـ:
- بناء التطبيق (Vite build)
- إنشاء ملفات التثبيت (NSIS و Portable)
- حفظ الملفات في مجلد `dist/`

### 3️⃣ إيقاف التطبيق
**استخدم الملف:** `stop-electron.bat`

أو من سطر الأوامر:
```bash
taskkill /IM electron.exe /F
```

## ملفات المشروع الجديدة

### `electron/main.js`
- ملف رئيسي لـ Electron
- يدير نافذة التطبيق
- يتعامل مع IPC (Inter-Process Communication)

### `electron/preload.js`
- ملف preload آمن
- يوفر واجهة آمنة بين العمليات
- يحمي من الثغرات الأمنية

### `start-electron.bat`
- ملف batch لتشغيل التطبيق في وضع التطوير

### `build-electron.bat`
- ملف batch لبناء التطبيق للإنتاج

### `stop-electron.bat`
- ملف batch لإيقاف التطبيق

## الخصائص المحفوظة

✅ جميع خصائص التطبيق الأصلية محفوظة:
- نفس الواجهة والتصميم
- نفس الوظائف والميزات
- نفس قاعدة البيانات والاتصالات
- نفس الإعدادات والتكوينات
- نفس الأداء والسرعة

## البناء والتوزيع

### ملفات التثبيت المُنتجة:
1. **NSIS Installer** - برنامج تثبيت احترافي
2. **Portable Executable** - نسخة محمولة بدون تثبيت

### موقع الملفات:
```
dist/
├── إدارة-المخزون-الاحترافية Setup 0.0.0.exe  (NSIS)
├── إدارة-المخزون-الاحترافية 0.0.0.exe        (Portable)
└── ...
```

## استكشاف الأخطاء

### المشكلة: لا يفتح التطبيق
**الحل:**
1. تأكد من تثبيت Node.js
2. قم بتشغيل `npm install` مرة أخرى
3. تحقق من أن المنفذ 5173 متاح

### المشكلة: خطأ في البناء
**الحل:**
1. احذف مجلد `node_modules`
2. احذف ملف `package-lock.json`
3. قم بتشغيل `npm install` مرة أخرى
4. حاول البناء مرة أخرى

### المشكلة: التطبيق بطيء
**الحل:**
1. تأكد من إغلاق جميع نسخ التطبيق
2. أعد تشغيل الكمبيوتر
3. تحقق من موارد النظام

## الأوامر المتاحة

```bash
# تشغيل في وضع التطوير (ويب)
npm run dev

# تشغيل في وضع التطوير (Electron)
npm run dev:electron

# بناء للويب
npm run build

# بناء لـ Electron
npm run build:electron

# معاينة البناء
npm run preview

# فحص حالة التطبيق
npm run status

# إيقاف التطبيق
npm run stop
```

## ملاحظات مهمة

⚠️ **عند التطوير:**
- استخدم `npm run dev:electron` لتطوير التطبيق
- أدوات المطور متفعلة تلقائياً
- التطبيق سيعيد التحميل عند حفظ الملفات

⚠️ **عند البناء:**
- تأكد من أن جميع الملفات محفوظة
- قد يستغرق البناء بضع دقائق
- تحقق من مساحة التخزين المتاحة

## الدعم والمساعدة

للمزيد من المعلومات:
- [Electron Documentation](https://www.electronjs.org/docs)
- [Vite Documentation](https://vitejs.dev/)
- [React Documentation](https://react.dev/)

---

**تم إنشاؤه بواسطة:** نظام إدارة المخزون الاحترافي
**الإصدار:** 0.0.0
**آخر تحديث:** 2025

