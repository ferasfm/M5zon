# 🚀 البدء السريع - تطبيق Electron

## ✅ تم إعداد كل شيء بنجاح!

تم تحويل التطبيق إلى تطبيق سطح مكتب احترافي باستخدام **Electron**.

---

## 🎯 طرق التشغيل

### الطريقة 1️⃣: استخدام ملف Batch (الأسهل)

#### تشغيل التطبيق:
```
انقر مرتين على: start-electron.bat
```

#### بناء التطبيق:
```
انقر مرتين على: build-electron.bat
```

#### إيقاف التطبيق:
```
انقر مرتين على: stop-electron.bat
```

---

### الطريقة 2️⃣: استخدام سطر الأوامر

#### فتح Command Prompt أو PowerShell في مجلد المشروع

#### تشغيل التطبيق:
```bash
npm run dev:electron
```

#### بناء التطبيق:
```bash
npm run build:electron
```

#### إيقاف التطبيق:
```bash
taskkill /IM electron.exe /F
```

---

## 📋 ما تم إضافته

✅ **ملفات Electron:**
- `electron/main.js` - ملف رئيسي لـ Electron
- `electron/preload.js` - ملف preload آمن

✅ **ملفات Batch:**
- `start-electron.bat` - لتشغيل التطبيق
- `build-electron.bat` - لبناء التطبيق
- `stop-electron.bat` - لإيقاف التطبيق

✅ **تحديثات:**
- `package.json` - إضافة scripts و dependencies
- `vite.config.ts` - تكوين Vite للعمل مع Electron

✅ **التوثيق:**
- `ELECTRON_SETUP.md` - دليل شامل
- `QUICK_START_ELECTRON.md` - هذا الملف

---

## 🎨 الخصائص المحفوظة

✅ **جميع الخصائص الأصلية محفوظة:**
- الواجهة والتصميم
- الوظائف والميزات
- قاعدة البيانات والاتصالات
- الإعدادات والتكوينات
- الأداء والسرعة

---

## 🔧 المتطلبات

- **Node.js** (الإصدار 14 أو أحدث)
- **npm** (يأتي مع Node.js)

### التحقق من التثبيت:
```bash
node --version
npm --version
```

---

## 📦 ملفات التثبيت

بعد تشغيل `build-electron.bat`، ستجد:

```
dist/
├── إدارة-المخزون-الاحترافية Setup 0.0.0.exe  (برنامج تثبيت)
├── إدارة-المخزون-الاحترافية 0.0.0.exe        (نسخة محمولة)
└── ...
```

---

## ⚡ نصائح مهمة

### عند التطوير:
- استخدم `start-electron.bat` لتشغيل التطبيق
- أدوات المطور متفعلة تلقائياً (F12)
- التطبيق سيعيد التحميل عند حفظ الملفات

### عند البناء:
- تأكد من إغلاق التطبيق أولاً
- قد يستغرق البناء بضع دقائق
- تحقق من مساحة التخزين المتاحة

### عند الإيقاف:
- استخدم `stop-electron.bat` أو أغلق النافذة مباشرة
- أو اضغط `Ctrl+C` في سطر الأوامر

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا يفتح التطبيق
**الحل:**
1. تأكد من تثبيت Node.js
2. قم بتشغيل `npm install` مرة أخرى
3. أعد تشغيل الكمبيوتر

### المشكلة: خطأ في البناء
**الحل:**
1. احذف مجلد `node_modules`
2. قم بتشغيل `npm install` مرة أخرى
3. حاول البناء مرة أخرى

### المشكلة: التطبيق بطيء
**الحل:**
1. أغلق جميع نسخ التطبيق
2. أعد تشغيل الكمبيوتر
3. تحقق من موارد النظام

---

## 📚 المزيد من المعلومات

للمزيد من التفاصيل، اقرأ: `ELECTRON_SETUP.md`

---

## 🎉 تم بنجاح!

التطبيق جاهز للاستخدام كتطبيق سطح مكتب احترافي!

**استمتع باستخدام نظام إدارة المخزون الاحترافي! 🚀**

