# دليل البدء السريع - نظام المصادقة

## الخطوات الأساسية

### 1. إعداد قاعدة البيانات
```bash
# تشغيل سكريبت الإعداد
node scripts/setup-auth-system.js
```

هذا السكريبت سيقوم بـ:
- ✅ إنشاء جدول المستخدمين
- ✅ إنشاء جدول الصلاحيات
- ✅ إنشاء جدول إعادة تعيين كلمات المرور
- ✅ إضافة مستخدم مدير افتراضي
- ✅ تعيين صلاحيات كاملة للمدير

### 2. تسجيل الدخول لأول مرة

**بيانات المدير الافتراضي:**
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

**خطوات تسجيل الدخول:**
1. افتح التطبيق
2. أدخل `admin` في حقل اسم المستخدم
3. أدخل `admin123` في حقل كلمة المرور
4. اضغط "تسجيل الدخول"
5. ستظهر نافذة تغيير كلمة المرور (إجباري)
6. أدخل كلمة مرور جديدة قوية (8 أحرف على الأقل)

### 3. إضافة مستخدمين جدد

1. من القائمة الجانبية، اختر "إدارة المستخدمين"
2. اضغط على زر "إضافة مستخدم جديد"
3. املأ البيانات:
   - اسم المستخدم (مطلوب، فريد)
   - الاسم الكامل (مطلوب)
   - البريد الإلكتروني (اختياري)
   - كلمة المرور (8 أحرف على الأقل)
   - الدور (admin/manager/user)
4. حدد الصلاحيات لكل صفحة
5. اضغط "حفظ"

### 4. الأدوار والصلاحيات

#### مدير النظام (admin)
- ✅ صلاحيات كاملة على جميع الصفحات
- ✅ إدارة المستخدمين
- ✅ تعيين الصلاحيات

#### مدير (manager)
- ✅ صلاحيات كاملة على معظم الصفحات
- ❌ لا يمكنه إدارة المستخدمين

#### مستخدم (user)
- ⚠️ صلاحيات محدودة حسب التعيين
- ✅ عرض فقط في معظم الصفحات

### 5. تعديل الصلاحيات

1. اذهب إلى "إدارة المستخدمين"
2. اضغط على "تعديل" بجانب المستخدم
3. في قسم "الصلاحيات"، حدد:
   - ✅ عرض - للسماح بعرض الصفحة
   - ✅ إنشاء - للسماح بإضافة بيانات جديدة
   - ✅ تعديل - للسماح بتعديل البيانات
   - ✅ حذف - للسماح بحذف البيانات
4. اضغط "حفظ"

### 6. إعادة تعيين كلمة المرور

**للمستخدم نفسه:**
1. من القائمة الجانبية، اضغط على "الإعدادات"
2. اختر "تغيير كلمة المرور"
3. أدخل كلمة المرور القديمة
4. أدخل كلمة المرور الجديدة
5. أكد كلمة المرور الجديدة
6. اضغط "حفظ"

**للمدير (إعادة تعيين لمستخدم آخر):**
1. اذهب إلى "إدارة المستخدمين"
2. اضغط على "إعادة تعيين كلمة المرور" بجانب المستخدم
3. أدخل كلمة المرور الجديدة
4. سيتم إجبار المستخدم على تغيير كلمة المرور عند تسجيل الدخول التالي

### 7. تسجيل الخروج

- اضغط على زر "تسجيل الخروج" في أسفل القائمة الجانبية
- سيتم إعادة توجيهك إلى صفحة تسجيل الدخول

## نصائح الأمان

### كلمات المرور القوية
- ✅ 8 أحرف على الأقل
- ✅ استخدم مزيج من الأحرف الكبيرة والصغيرة
- ✅ أضف أرقام ورموز خاصة
- ❌ لا تستخدم كلمات مرور سهلة التخمين

### أفضل الممارسات
- 🔒 غير كلمة المرور الافتراضية فوراً
- 🔒 لا تشارك كلمات المرور
- 🔒 استخدم كلمة مرور مختلفة لكل مستخدم
- 🔒 قم بمراجعة الصلاحيات بشكل دوري
- 🔒 عطّل الحسابات غير المستخدمة

## استكشاف الأخطاء الشائعة

### ❌ "خطأ في اسم المستخدم أو كلمة المرور"
- تحقق من كتابة اسم المستخدم بشكل صحيح
- تأكد من كتابة كلمة المرور بشكل صحيح
- تحقق من أن الحساب مفعّل

### ❌ "ليس لديك صلاحية للوصول إلى هذه الصفحة"
- تواصل مع المدير لتعيين الصلاحيات المناسبة
- تحقق من دورك في النظام

### ❌ "خطأ في الاتصال بقاعدة البيانات"
- تأكد من تشغيل خادم PostgreSQL
- تحقق من إعدادات الاتصال
- راجع ملف التكوين

### ❌ "كلمة المرور ضعيفة"
- استخدم 8 أحرف على الأقل
- أضف أحرف كبيرة وصغيرة
- أضف أرقام أو رموز خاصة

## الصفحات المتاحة

| الصفحة | الوصف | الصلاحية الافتراضية |
|--------|-------|---------------------|
| لوحة التحكم | عرض الإحصائيات | الجميع |
| المنتجات | إدارة المنتجات | مدير+ |
| استلام البضاعة | تسجيل الاستلام | مدير+ |
| صرف البضاعة | تسجيل الصرف | مدير+ |
| إدارة التسليمات | متابعة التسليمات | مدير+ |
| إتلاف البضاعة | تسجيل الإتلاف | مدير+ |
| الموردون | إدارة الموردين | مدير+ |
| المواقع والعملاء | إدارة المواقع | مدير+ |
| التقارير | عرض التقارير | الجميع |
| المطالبات المالية | طباعة المطالبات | مدير+ |
| إدارة المستخدمين | إدارة الحسابات | مدير النظام فقط |
| الإعدادات | إعدادات التطبيق | الجميع |

## الدعم

إذا واجهت أي مشاكل:
1. راجع دليل استكشاف الأخطاء أعلاه
2. تحقق من ملف `AUTH_SYSTEM_GUIDE.md` للتفاصيل الكاملة
3. تواصل مع فريق التطوير

---

**ملاحظة مهمة:** هذا النظام مصمم لحماية بياناتك. تأكد من اتباع أفضل ممارسات الأمان!
