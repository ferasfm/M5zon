# دليل تشغيل التطبيق 🚀

## تشغيل التطبيق على الويب

### الطريقة 1: استخدام ملف BAT
```bash
start.bat
```

### الطريقة 2: استخدام NPM
```bash
npm run dev
```

سيعمل التطبيق على: `http://localhost:5173`

---

## تشغيل تطبيق سطح المكتب (Electron)

### الطريقة 1: استخدام ملف BAT
```bash
start-electron.bat
```

### الطريقة 2: استخدام NPM
```bash
npm run electron:dev
```

---

## إيقاف التطبيق

### إيقاف تطبيق الويب
```bash
stop.bat
```

### إيقاف تطبيق سطح المكتب
```bash
stop-electron.bat
```

---

## بناء التطبيق للإنتاج

### بناء تطبيق الويب
```bash
npm run build
```

### بناء تطبيق سطح المكتب
```bash
npm run build:electron
```

سيتم إنشاء الملفات في مجلد `dist`:
- **m5zon Setup 1.0.0.exe** - ملف التثبيت
- **m5zon 1.0.0.exe** - النسخة المحمولة

---

## التثبيت الأولي

إذا كانت هذه أول مرة تشغل فيها المشروع:

```bash
npm install
```

---

## ملاحظات مهمة

- ✅ تأكد من تثبيت Node.js (الإصدار 16 أو أحدث)
- ✅ تأكد من إعداد قاعدة البيانات Supabase
- ✅ تحقق من ملف `.env` للإعدادات
- ✅ استخدم `status.bat` للتحقق من حالة الخوادم

---

## استكشاف الأخطاء

### المنفذ مشغول
```bash
# إيقاف جميع العمليات
stop.bat
stop-electron.bat

# ثم إعادة التشغيل
start.bat
```

### مشاكل في التثبيت
```bash
# حذف node_modules وإعادة التثبيت
rmdir /s /q node_modules
npm install
```

---

## الدعم

للمزيد من المعلومات، راجع:
- `README.md` - الوثائق الرئيسية
- `PRODUCT_NOTES_FEATURE.md` - ميزة الملاحظات الجديدة
