# 🖥️ تطبيق M5zon لإدارة المخزون - سطح المكتب

## 📖 نظرة عامة

تطبيق سطح مكتب احترافي لإدارة المخزون مع نظام مصادقة وصلاحيات متكامل.

### ✨ المميزات الرئيسية

- 🔐 **نظام مصادقة آمن** - تسجيل دخول وصلاحيات متقدمة
- 👥 **إدارة المستخدمين** - إضافة وتعديل المستخدمين والصلاحيات
- 📦 **إدارة المنتجات** - إضافة وتعديل المنتجات
- 📥 **استلام البضاعة** - تسجيل الاستلام من الموردين
- 📤 **صرف البضاعة** - تسجيل الصرف للعملاء
- 🚚 **إدارة التسليمات** - متابعة التسليمات
- 🗑️ **إتلاف البضاعة** - تسجيل البضاعة التالفة
- 🏢 **إدارة الموردين** - قاعدة بيانات الموردين
- 📍 **إدارة المواقع** - إدارة المواقع والعملاء
- 📊 **التقارير** - تقارير شاملة ومفصلة
- 💰 **المطالبات المالية** - طباعة المطالبات
- ⚙️ **الإعدادات** - تخصيص التطبيق

## 🚀 البدء السريع

### للمطورين:

#### 1. التثبيت
```bash
npm install
```

#### 2. إعداد قاعدة البيانات
```bash
# استخدم السكريبت الجاهز
setup-database.bat

# أو يدوياً
node scripts/setup-auth-system.js
```

#### 3. الاختبار
```bash
# استخدم السكريبت الجاهز
test-app.bat

# أو يدوياً
npm run dev              # في المتصفح
npm run dev:electron     # في Electron
```

#### 4. البناء
```bash
# استخدم السكريبت الجاهز
build-app.bat

# أو يدوياً
npm run build
npm run build:electron
```

### للمستخدمين:

راجع ملف `USER_INSTALLATION_GUIDE.md` للحصول على دليل التثبيت الكامل.

## 📁 هيكل المشروع

```
m5zon/
├── 📁 components/           # مكونات React
│   ├── Login.tsx           # صفحة تسجيل الدخول
│   ├── UsersManagement.tsx # إدارة المستخدمين
│   ├── ProtectedRoute.tsx  # حماية الصفحات
│   └── ...
├── 📁 contexts/            # سياقات React
│   ├── AuthContext.tsx     # سياق المصادقة
│   ├── PermissionsContext.tsx # سياق الصلاحيات
│   └── ...
├── 📁 electron/            # ملفات Electron
│   ├── main.js            # الملف الرئيسي
│   ├── preload.cjs        # Preload script
│   └── database.js        # إدارة قاعدة البيانات
├── 📁 scripts/            # سكريبتات مساعدة
│   ├── setup-auth-system.js # إعداد نظام المصادقة
│   └── ...
├── 📁 supabase/           # ملفات SQL
│   ├── create_users_table.sql
│   ├── create_permissions_table.sql
│   └── ...
├── 📁 public/             # ملفات عامة
│   └── icon.ico          # أيقونة التطبيق
├── 📄 package.json        # إعدادات المشروع
├── 📄 vite.config.ts      # إعدادات Vite
└── 📄 README_DESKTOP.md   # هذا الملف
```

## 🔐 نظام المصادقة

### الأدوار المتاحة:

1. **Admin (مدير النظام)**
   - ✅ صلاحيات كاملة على جميع الصفحات
   - ✅ إدارة المستخدمين
   - ✅ تعيين الصلاحيات

2. **Manager (مدير)**
   - ✅ صلاحيات كاملة على معظم الصفحات
   - ❌ لا يمكنه إدارة المستخدمين

3. **User (مستخدم)**
   - ⚠️ صلاحيات محدودة حسب التعيين
   - ✅ عرض فقط في معظم الصفحات

### بيانات الدخول الافتراضية:
```
اسم المستخدم: admin
كلمة المرور: admin123
```

⚠️ **مهم:** يجب تغيير كلمة المرور عند أول تسجيل دخول!

## 📊 قاعدة البيانات

### الجداول الرئيسية:

- `users` - المستخدمون
- `permissions` - الصلاحيات
- `password_resets` - إعادة تعيين كلمات المرور
- `products` - المنتجات
- `inventory_items` - المخزون
- `suppliers` - الموردون
- `locations` - المواقع
- `categories` - التصنيفات
- `reasons` - الأسباب

## 🛠️ التقنيات المستخدمة

### Frontend:
- ⚛️ React 19
- 🎨 Tailwind CSS
- 📊 Recharts
- 🎯 TypeScript

### Backend:
- 🗄️ PostgreSQL
- 🔐 bcryptjs (تشفير كلمات المرور)
- 🔌 Supabase Client

### Desktop:
- 🖥️ Electron
- 📦 Electron Builder
- ⚡ Vite

## 📝 الأوامر المتاحة

### التطوير:
```bash
npm run dev              # تشغيل في المتصفح
npm run dev:electron     # تشغيل في Electron
npm run dev:vite         # تشغيل Vite فقط
```

### البناء:
```bash
npm run build            # بناء ملفات الويب
npm run build:electron   # بناء تطبيق سطح المكتب
npm run build:installer  # بناء Installer فقط
npm run build:portable   # بناء Portable فقط
```

### الإدارة:
```bash
npm run start            # تشغيل الخادم
npm run stop             # إيقاف الخادم
npm run status           # حالة الخادم
npm run restart          # إعادة تشغيل الخادم
```

### قاعدة البيانات:
```bash
node scripts/setup-auth-system.js  # إعداد نظام المصادقة
```

## 📚 الوثائق

### للمطورين:
- 📖 `BUILD_DESKTOP_APP.md` - دليل البناء الكامل
- ⚡ `QUICK_BUILD_GUIDE.md` - دليل البناء السريع
- 💻 `AUTH_DEVELOPER_NOTES.md` - ملاحظات المطورين
- 📋 `AUTH_CHECKLIST.md` - قائمة التحقق

### للمستخدمين:
- 📱 `USER_INSTALLATION_GUIDE.md` - دليل التثبيت
- 🚀 `QUICK_START_AUTH.md` - دليل البدء السريع
- 📖 `AUTH_SYSTEM_GUIDE.md` - دليل نظام المصادقة

### الملخصات:
- ✅ `AUTH_IMPLEMENTATION_SUMMARY.md` - ملخص التطبيق

## 🔧 حل المشاكل

### خطأ في البناء:
```bash
rmdir /s /q dist node_modules
npm install
npm run build:electron
```

### خطأ في قاعدة البيانات:
```bash
# تحقق من تشغيل PostgreSQL
# راجع معلومات الاتصال
node scripts/setup-auth-system.js
```

### التطبيق لا يعمل:
```bash
# أعد بناء ملفات الويب أولاً
npm run build
npm run build:electron
```

## 🎯 متطلبات النظام

### للتطوير:
- Node.js 16+
- npm أو yarn
- PostgreSQL 12+
- Windows 10/11

### للمستخدمين:
- Windows 10/11 (64-bit)
- 4 GB RAM (8 GB موصى به)
- 500 MB مساحة فارغة
- PostgreSQL 12+

## 📞 الدعم

### للحصول على المساعدة:
- 📧 البريد الإلكتروني: info@m5zon.com
- 📱 الهاتف: [رقم الهاتف]
- 🌐 الموقع: [رابط الموقع]

## 📄 الترخيص

هذا المشروع ملك لشركة M5zon. جميع الحقوق محفوظة.

## 🙏 شكر وتقدير

شكراً لاستخدام تطبيق M5zon لإدارة المخزون!

---

**نسخة:** 1.0.0  
**تاريخ التحديث:** ديسمبر 2024  
**الحالة:** ✅ جاهز للإنتاج
