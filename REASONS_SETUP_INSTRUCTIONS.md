# تعليمات إعداد نظام أسباب الشراء والصرف

## ✅ ما تم إنجازه:

### 1. قاعدة البيانات
- ✅ تم إنشاء ملف SQL: `supabase/add_reasons_table.sql`
- ✅ يحتوي على جدول `transaction_reasons`
- ✅ يحتوي على البيانات الافتراضية (احتياج جديد، بدل تالف، تجديد مخزون، مكسور)

### 2. الكود
- ✅ تم تحديث `types.ts` لإضافة `TransactionReason`
- ✅ تم تحديث `useInventory.ts` لإضافة API كامل لإدارة الأسباب
- ✅ تم إنشاء مكون `ReasonsManager.tsx` لإدارة الأسباب من الإعدادات
- ✅ تم إضافة المكون إلى صفحة `Settings.tsx`
- ✅ تم تحديث `Receiving.tsx` لاستخدام الأسباب الديناميكية
- ✅ تم تحديث `Dispatching.tsx` لاستخدام الأسباب الديناميكية
- ✅ تم إضافة الأيقونات المطلوبة (Eye, EyeOff, Save)

## 🔧 خطوات التشغيل:

### الخطوة 1: تشغيل SQL في Supabase

1. افتح لوحة تحكم Supabase الخاصة بك
2. اذهب إلى **SQL Editor**
3. انسخ محتوى ملف `supabase/add_reasons_table.sql`
4. الصق المحتوى في المحرر
5. اضغط **Run** أو **F5**

### الخطوة 2: التحقق من نجاح العملية

بعد تشغيل SQL، تحقق من:
- ✅ تم إنشاء جدول `transaction_reasons`
- ✅ تم إدراج 4 أسباب افتراضية

### الخطوة 3: اختبار النظام

1. افتح البرنامج
2. اذهب إلى **الإعدادات**
3. ستجد قسم جديد: **إدارة أسباب الشراء والصرف**
4. جرب:
   - إضافة سبب جديد
   - تعديل سبب موجود
   - تفعيل/تعطيل سبب
   - حذف سبب

5. اذهب إلى **استلام بضاعة**
   - ستجد قائمة "سبب الشراء" تحتوي على الأسباب من قاعدة البيانات

6. اذهب إلى **صرف بضاعة**
   - ستجد قائمة "سبب الصرف" تحتوي على الأسباب من قاعدة البيانات

## 📊 كيف يعمل النظام:

### أنواع الأسباب:
- **شراء فقط**: يظهر في صفحة الاستلام فقط
- **صرف فقط**: يظهر في صفحة الصرف فقط
- **شراء وصرف**: يظهر في كلا الصفحتين

### الميزات:
- ✅ إضافة أسباب جديدة
- ✅ تعديل الأسباب الموجودة
- ✅ تفعيل/تعطيل الأسباب (بدون حذفها)
- ✅ حذف الأسباب نهائيًا
- ✅ ترتيب الأسباب تلقائيًا
- ✅ التحديث الفوري في جميع صفحات البرنامج

## 🔗 العلاقات مع قاعدة البيانات:

```
transaction_reasons (جدول الأسباب)
├── id (UUID)
├── reason_text (نص السبب)
├── reason_type (نوع: purchase/dispatch/both)
├── is_active (فعال/معطل)
└── display_order (ترتيب العرض)

inventory_items (جدول المخزون)
├── purchase_reason (يحفظ نص السبب)
└── dispatch_reason (يحفظ نص السبب)
```

**ملاحظة**: الأسباب تُحفظ كنص في جدول المخزون، وليس كـ foreign key، لضمان عدم فقدان البيانات التاريخية عند حذف سبب.

## ❓ استكشاف الأخطاء:

### إذا لم تظهر الأسباب:
1. تأكد من تشغيل SQL في Supabase
2. تحقق من Console في المتصفح (F12)
3. تأكد من اتصال Supabase

### إذا ظهرت أخطاء:
1. تحقق من أن جدول `transaction_reasons` موجود
2. تحقق من صلاحيات Supabase (RLS)
3. راجع رسائل الخطأ في Console

## 🎉 انتهى!

الآن يمكنك إدارة أسباب الشراء والصرف بسهولة من صفحة الإعدادات، وستنعكس التغييرات فورًا على جميع صفحات البرنامج!
