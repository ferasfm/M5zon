# حل مشكلة عدم ظهور صفحة تسجيل الدخول 🔧

## المشكلة:
عند تشغيل البرنامج، لا تظهر صفحة تسجيل الدخول ويدخل مباشرة للتطبيق.

## السبب:
التطبيق يحفظ الجلسة في localStorage، وعند إعادة فتحه يسجل الدخول تلقائياً.

## الحلول:

### الحل 1: مسح الجلسة المحفوظة (الأسرع) ⚡

#### في المتصفح:
1. افتح Developer Tools (اضغط F12)
2. اذهب إلى تبويب "Console"
3. اكتب الأمر التالي واضغط Enter:
```javascript
localStorage.clear()
```
4. أعد تحميل الصفحة (F5)

#### في Electron:
1. افتح Developer Tools (اضغط F12 أو Ctrl+Shift+I)
2. اذهب إلى تبويب "Console"
3. اكتب الأمر التالي واضغط Enter:
```javascript
localStorage.clear()
```
4. أعد تشغيل التطبيق

### الحل 2: استخدام زر تسجيل الخروج 🚪

1. ابحث عن زر "تسجيل الخروج" في أسفل القائمة الجانبية
2. اضغط عليه
3. ستظهر صفحة تسجيل الدخول

### الحل 3: حذف ملفات التطبيق المؤقتة 🗑️

#### Windows:
```bash
# احذف مجلد بيانات التطبيق
rmdir /s /q "%APPDATA%\m5zon-inventory-pro"
```

### الحل 4: إعادة تعيين قاعدة البيانات 🔄

إذا لم تكن قد قمت بإعداد قاعدة البيانات بعد:

```bash
# شغّل سكريبت الإعداد
node scripts/setup-auth-system.js
```

هذا سيقوم بـ:
- ✅ إنشاء جداول المستخدمين
- ✅ إضافة مستخدم admin افتراضي
- ✅ تعيين الصلاحيات

## التحقق من نجاح الحل:

بعد تطبيق أي من الحلول أعلاه:

1. ✅ يجب أن تظهر صفحة تسجيل الدخول
2. ✅ أدخل: `admin` / `admin123`
3. ✅ يجب أن تظهر نافذة تغيير كلمة المرور

## ملاحظات مهمة:

### سلوك التطبيق الطبيعي:
- ✅ **أول مرة**: تظهر صفحة تسجيل الدخول
- ✅ **بعد تسجيل الدخول**: يحفظ الجلسة
- ✅ **عند إعادة فتح التطبيق**: يدخل تلقائياً (هذا طبيعي!)
- ✅ **بعد تسجيل الخروج**: تظهر صفحة تسجيل الدخول مرة أخرى

### إذا أردت إجبار ظهور صفحة تسجيل الدخول دائماً:

يمكنك تعطيل حفظ الجلسة بتعديل `contexts/AuthContext.tsx`:

```typescript
// ابحث عن هذا السطر:
localStorage.setItem('currentUser', JSON.stringify(userData));

// وعلّق عليه:
// localStorage.setItem('currentUser', JSON.stringify(userData));
```

⚠️ **لكن هذا سيجبر المستخدم على تسجيل الدخول في كل مرة!**

## اختبار نظام المصادقة:

### 1. اختبار تسجيل الدخول:
```
اسم المستخدم: admin
كلمة المرور: admin123
```

### 2. اختبار تغيير كلمة المرور:
- يجب أن تظهر نافذة تغيير كلمة المرور تلقائياً
- أدخل كلمة مرور جديدة (8 أحرف على الأقل)

### 3. اختبار تسجيل الخروج:
- اضغط على زر "تسجيل الخروج" في القائمة الجانبية
- يجب أن تظهر صفحة تسجيل الدخول

### 4. اختبار الجلسة:
- سجل الدخول
- أغلق التطبيق
- افتح التطبيق مرة أخرى
- يجب أن يدخل تلقائياً (هذا صحيح!)

## الأسئلة الشائعة:

### س: لماذا لا تظهر صفحة تسجيل الدخول؟
**ج:** لأن الجلسة محفوظة. هذا سلوك طبيعي لتحسين تجربة المستخدم.

### س: كيف أجبر ظهور صفحة تسجيل الدخول؟
**ج:** اضغط على زر "تسجيل الخروج" أو امسح localStorage.

### س: هل هذا آمن؟
**ج:** نعم! الجلسة محفوظة محلياً فقط، وكلمات المرور مشفرة في قاعدة البيانات.

### س: كيف أختبر مع مستخدمين مختلفين؟
**ج:** 
1. سجل الخروج
2. سجل الدخول بمستخدم آخر
3. أو استخدم نافذة متصفح خاصة (Incognito)

## للمطورين:

### تعطيل حفظ الجلسة مؤقتاً:

في `contexts/AuthContext.tsx`:

```typescript
// في useEffect، علّق على هذا الجزء:
/*
const savedUser = localStorage.getItem('currentUser');
if (savedUser) {
    try {
        const userData = JSON.parse(savedUser);
        // ...
    } catch (error) {
        localStorage.removeItem('currentUser');
    }
}
*/
```

### إضافة زر "مسح الجلسة" للتطوير:

```typescript
// في أي مكون
<button onClick={() => {
    localStorage.clear();
    window.location.reload();
}}>
    مسح الجلسة
</button>
```

## الخلاصة:

✅ **السلوك الحالي صحيح ومقصود!**

التطبيق يحفظ الجلسة لتحسين تجربة المستخدم. إذا أردت رؤية صفحة تسجيل الدخول:
1. اضغط "تسجيل الخروج"
2. أو امسح localStorage
3. أو استخدم نافذة خاصة

---

**هل تحتاج مساعدة إضافية؟** راجع `AUTH_SYSTEM_GUIDE.md`
