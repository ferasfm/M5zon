# 🗄️ معلومات قاعدة البيانات

## الإعدادات الحالية المستخدمة في التطبيق

### 📍 معلومات الاتصال الافتراضية

```
عنوان السيرفر (Host): 172.10.0.16
المنفذ (Port): 5432
اسم قاعدة البيانات: m5zon_local
اسم المستخدم: postgres
كلمة المرور: (فارغة في واجهة الإدخال)
```

### 🔐 كلمة المرور المستخدمة في السكريبتات

في ملفات السكريبتات تم استخدام:
```
كلمة المرور: P@$$w0rd@1234
```

**ملاحظة:** تأكد من استخدام `$$` وليس `$` واحد فقط

---

## 📂 الملفات التي تحتوي على معلومات قاعدة البيانات

1. **components/LocalConnectionSettings.tsx** - واجهة إدخال الإعدادات
2. **contexts/SupabaseContext.tsx** - إدارة الاتصال
3. **scripts/test-local-db.js** - سكريبت الاختبار
4. **scripts/check-products-categories.js** - سكريبت فحص المنتجات

---

## 🔍 كيفية معرفة الإعدادات المحفوظة حالياً

الإعدادات محفوظة في `localStorage` في المتصفح تحت المفتاح:
```
localDbConfig
```

لعرضها:
1. افتح Developer Tools (F12)
2. اذهب إلى Console
3. اكتب: `localStorage.getItem('localDbConfig')`

---

## ⚙️ تغيير الإعدادات

لتغيير إعدادات الاتصال:
1. احذف الملف المحفوظ: `localStorage.removeItem('localDbConfig')`
2. أعد تشغيل التطبيق
3. ستظهر شاشة إدخال الإعدادات من جديد

---

## 🔧 اختبار الاتصال

لاختبار الاتصال بقاعدة البيانات:
```bash
npm run db:test
```

أو قم بتشغيل:
```bash
node scripts/test-local-db.js
```
