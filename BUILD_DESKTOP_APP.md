# دليل بناء تطبيق سطح المكتب 🖥️

## 📋 المتطلبات الأساسية

### قبل البدء، تأكد من تثبيت:
- ✅ Node.js (v16 أو أحدث)
- ✅ npm أو yarn
- ✅ PostgreSQL (للقاعدة البيانات المحلية)
- ✅ Git (اختياري)

## 🚀 خطوات البناء

### 1️⃣ إعداد المشروع

```bash
# تثبيت جميع المكتبات
npm install

# أو إذا كنت تستخدم yarn
yarn install
```

### 2️⃣ إعداد قاعدة البيانات

```bash
# تشغيل سكريبت إعداد نظام المصادقة
node scripts/setup-auth-system.js
```

هذا سيقوم بـ:
- ✅ إنشاء جدول المستخدمين
- ✅ إنشاء جدول الصلاحيات
- ✅ إضافة مستخدم admin افتراضي
- ✅ تعيين الصلاحيات الافتراضية

### 3️⃣ اختبار التطبيق في وضع التطوير

```bash
# تشغيل التطبيق في المتصفح
npm run dev

# أو تشغيل في Electron مباشرة
npm run dev:electron
```

**بيانات تسجيل الدخول الافتراضية:**
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

### 4️⃣ بناء التطبيق

#### أ) بناء ملفات الويب أولاً:
```bash
npm run build
```

هذا سيُنشئ مجلد `dist` يحتوي على ملفات التطبيق المحسّنة.

#### ب) بناء تطبيق سطح المكتب:

**خيار 1: بناء كامل (Installer + Portable)**
```bash
npm run build:electron
```

**خيار 2: بناء Installer فقط**
```bash
npm run build:installer
```

**خيار 3: بناء Portable فقط**
```bash
npm run build:portable
```

### 5️⃣ العثور على الملفات المبنية

بعد اكتمال البناء، ستجد الملفات في مجلد `dist`:

```
dist/
├── m5zon Setup 1.0.0.exe          ← ملف التثبيت (Installer)
├── m5zon 1.0.0.exe                ← النسخة المحمولة (Portable)
├── win-unpacked/                  ← ملفات التطبيق غير المضغوطة
└── ...
```

## 📦 أنواع الملفات المبنية

### 1. **Installer (NSIS)**
- **الملف**: `m5zon Setup 1.0.0.exe`
- **الحجم**: ~150-200 MB
- **المميزات**:
  - ✅ تثبيت احترافي
  - ✅ اختصار على سطح المكتب
  - ✅ اختصار في قائمة ابدأ
  - ✅ إمكانية إلغاء التثبيت
  - ✅ تحديثات تلقائية (إذا تم تفعيلها)

### 2. **Portable**
- **الملف**: `m5zon 1.0.0.exe`
- **الحجم**: ~150-200 MB
- **المميزات**:
  - ✅ لا يحتاج تثبيت
  - ✅ يعمل مباشرة
  - ✅ يمكن نقله على USB
  - ✅ لا يترك آثار في النظام

## ⚙️ إعدادات البناء

### تخصيص معلومات التطبيق

في `package.json`:

```json
{
  "name": "m5zon-inventory-pro",
  "version": "1.0.0",
  "description": "نظام إدارة المخزون الاحترافي",
  "author": {
    "name": "M5zon",
    "email": "info@m5zon.com"
  }
}
```

### تخصيص إعدادات Electron Builder

في `package.json` تحت `build`:

```json
{
  "build": {
    "appId": "com.m5zon.inventory",
    "productName": "m5zon",
    "win": {
      "target": ["nsis", "portable"],
      "icon": "public/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  }
}
```

## 🎨 تخصيص الأيقونة

### إنشاء أيقونة مخصصة:

1. **إنشاء صورة PNG بحجم 512x512**
2. **تحويلها إلى ICO** باستخدام أداة مثل:
   - https://convertio.co/png-ico/
   - https://icoconvert.com/

3. **حفظ الملف** في `public/icon.ico`

4. **تحديث package.json**:
```json
{
  "build": {
    "win": {
      "icon": "public/icon.ico"
    }
  }
}
```

## 🔧 حل المشاكل الشائعة

### مشكلة 1: خطأ في البناء
```bash
# حذف المجلدات القديمة
rmdir /s /q dist
rmdir /s /q node_modules

# إعادة التثبيت
npm install

# إعادة البناء
npm run build:electron
```

### مشكلة 2: التطبيق لا يعمل بعد البناء
```bash
# تأكد من بناء ملفات الويب أولاً
npm run build

# ثم بناء Electron
npm run build:electron
```

### مشكلة 3: الأيقونة لا تظهر
- تأكد من وجود `icon.ico` في مجلد `public`
- تأكد من أن الأيقونة بصيغة ICO وليست PNG
- أعد البناء بعد إضافة الأيقونة

### مشكلة 4: حجم الملف كبير جداً
هذا طبيعي! التطبيق يحتوي على:
- Node.js runtime
- Chromium engine
- جميع المكتبات
- ملفات التطبيق

**الحجم المتوقع**: 150-250 MB

## 📊 تحسين حجم التطبيق

### 1. إزالة المكتبات غير المستخدمة
```bash
npm prune --production
```

### 2. تفعيل الضغط
في `package.json`:
```json
{
  "build": {
    "compression": "maximum",
    "asar": true
  }
}
```

### 3. استبعاد ملفات غير ضرورية
```json
{
  "build": {
    "files": [
      "dist/**/*",
      "electron/**/*",
      "!node_modules/**/{CHANGELOG.md,README.md,*.map}"
    ]
  }
}
```

## 🚀 نشر التطبيق

### 1. **التوزيع المحلي**
- انسخ ملف `m5zon Setup 1.0.0.exe` أو `m5zon 1.0.0.exe`
- شاركه مع المستخدمين
- يمكن نقله على USB أو رفعه على السحابة

### 2. **التوزيع عبر الإنترنت**
- ارفع الملف على Google Drive / Dropbox
- شارك الرابط مع المستخدمين
- أو استخدم GitHub Releases

### 3. **التحديثات التلقائية** (متقدم)
يمكن إضافة نظام تحديثات تلقائي باستخدام:
- electron-updater
- GitHub Releases
- خادم تحديثات خاص

## 📝 قائمة التحقق قبل النشر

- [ ] اختبار التطبيق في وضع التطوير
- [ ] اختبار جميع الميزات
- [ ] اختبار نظام المصادقة
- [ ] اختبار الاتصال بقاعدة البيانات
- [ ] تحديث رقم الإصدار في package.json
- [ ] إضافة أيقونة مخصصة
- [ ] بناء التطبيق
- [ ] اختبار الملف المبني على جهاز نظيف
- [ ] كتابة ملف README للمستخدمين
- [ ] إنشاء دليل التثبيت

## 📖 ملفات مهمة للمستخدمين

### إنشاء ملف README للمستخدمين:

```markdown
# تطبيق M5zon لإدارة المخزون

## التثبيت
1. قم بتحميل ملف `m5zon Setup 1.0.0.exe`
2. قم بتشغيل الملف
3. اتبع خطوات التثبيت
4. سيتم إنشاء اختصار على سطح المكتب

## المتطلبات
- Windows 10 أو أحدث
- PostgreSQL (للقاعدة البيانات المحلية)
- 4 GB RAM على الأقل
- 500 MB مساحة فارغة

## تسجيل الدخول الأول
- اسم المستخدم: admin
- كلمة المرور: admin123
- يجب تغيير كلمة المرور عند أول تسجيل دخول

## الدعم
للمساعدة، يرجى التواصل على: info@m5zon.com
```

## 🎯 الخطوات السريعة

```bash
# 1. تثبيت المكتبات
npm install

# 2. إعداد قاعدة البيانات
node scripts/setup-auth-system.js

# 3. اختبار التطبيق
npm run dev

# 4. بناء التطبيق
npm run build
npm run build:electron

# 5. العثور على الملف في مجلد dist
```

## ✨ نصائح إضافية

### للتطوير:
- استخدم `npm run dev:electron` للاختبار السريع
- راقب Console للأخطاء
- اختبر على أجهزة مختلفة

### للإنتاج:
- استخدم `npm run build:electron` للبناء النهائي
- اختبر الملف المبني قبل التوزيع
- احتفظ بنسخة احتياطية من الكود

### للأداء:
- قلل حجم الصور
- استخدم lazy loading
- قلل عدد المكتبات

## 🎉 تهانينا!

الآن لديك تطبيق سطح مكتب احترافي جاهز للتوزيع! 🚀

---

**ملاحظة**: هذا الدليل مخصص لنظام Windows. للبناء على Mac أو Linux، راجع وثائق electron-builder.
