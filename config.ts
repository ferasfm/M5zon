import { AppSettings } from './types';

/**
 * الإعدادات الرئيسية للتطبيق.
 * تم نقل الإعدادات إلى هذا الملف لتكون مركزية وثابتة عبر جميع الأجهزة والمتصفحات،
 * بناءً على طلبك. لتغيير هذه القيم، قم بتعديلها مباشرة هنا.
 */
export const appConfig: AppSettings = {
  /**
   * حد المخزون الذي يعتبر منخفضًا.
   * سيظهر تنبيه في لوحة التحكم للمنتجات التي يقل عددها في المخزون عن هذه القيمة.
   */
  lowStockThreshold: 5,

  /**
   * عدد الأيام قبل انتهاء الكفالة لإظهار تنبيه.
   * سيظهر تنبيه في لوحة التحكم للقطع التي ستنتهي كفالتها خلال هذه المدة.
   */
  warrantyDaysThreshold: 30,
};
