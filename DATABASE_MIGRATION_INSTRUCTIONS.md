# 🗄️ تعليمات تحديث قاعدة البيانات - دعم الحزم

## ⚠️ مهم جداً: يجب تنفيذ هذه الخطوة قبل استخدام ميزة الحزم

### الخطوات:

1. **افتح Supabase Dashboard**
   - اذهب إلى: https://supabase.com/dashboard
   - اختر مشروعك

2. **افتح SQL Editor**
   - من القائمة الجانبية، اضغط على "SQL Editor"
   - اضغط "New Query"

3. **انسخ والصق الكود التالي:**

```sql
-- إضافة أعمدة لتتبع الحزم في جدول inventory_items
-- هذا التحديث يسمح بتجميع القطع التي تنتمي لنفس الحزمة

-- إضافة عمود bundle_group_id لتتبع الحزم
ALTER TABLE inventory_items 
ADD COLUMN IF NOT EXISTS bundle_group_id TEXT;

-- إضافة عمود bundle_name لحفظ اسم الحزمة
ALTER TABLE inventory_items 
ADD COLUMN IF NOT EXISTS bundle_name TEXT;

-- إضافة فهرس لتحسين الأداء عند البحث بالحزم
CREATE INDEX IF NOT EXISTS idx_inventory_items_bundle_group 
ON inventory_items(bundle_group_id) 
WHERE bundle_group_id IS NOT NULL;

-- إضافة تعليق توضيحي
COMMENT ON COLUMN inventory_items.bundle_group_id IS 'معرف فريد يربط القطع التي تنتمي لنفس الحزمة';
COMMENT ON COLUMN inventory_items.bundle_name IS 'اسم الحزمة إذا كانت القطعة جزء من حزمة';
```

4. **اضغط "Run" أو F5**

5. **تأكد من النجاح**
   - يجب أن ترى رسالة "Success" أو "Completed"
   - إذا ظهرت أخطاء، تأكد من أنك متصل بالمشروع الصحيح

---

## ✅ ما الذي تم إضافته؟

### الأعمدة الجديدة:

1. **bundle_group_id** (نص اختياري)
   - معرف فريد يربط جميع القطع التي تنتمي لنفس الحزمة
   - مثال: `bundle_1732612345_abc123`
   - القطع العادية (غير الحزم) ستكون `NULL`

2. **bundle_name** (نص اختياري)
   - اسم الحزمة التي تنتمي إليها القطعة
   - مثال: "حزمة كاميرات المراقبة"
   - القطع العادية ستكون `NULL`

---

## 🎯 كيف يعمل النظام الجديد؟

### عند استلام حزمة:
1. تختار الحزمة من قائمة الحزم
2. تدخل الكمية (مثلاً: 2 حزمة)
3. النظام يجهز جميع المكونات
4. تدخل بار كود لكل قطعة
5. عند الحفظ، يتم:
   - توليد `bundle_group_id` فريد لكل حزمة
   - حفظ اسم الحزمة في كل قطعة
   - ربط جميع القطع بنفس المعرف

### في المطالبة المالية:
- **الحزم**: تظهر كسطر واحد باسم الحزمة والتكلفة الإجمالية
- **المنتجات العادية**: تظهر كما هي (مجمعة حسب المنتج)

---

## 📊 مثال عملي:

### قبل التحديث:
```
المنتج              | الكمية | السعر
-------------------|--------|-------
كاميرا مراقبة      | 2      | 500
DVR 8 قنوات        | 1      | 800
كابل 100 متر       | 1      | 200
```

### بعد التحديث (إذا كانت حزمة):
```
المنتج              | الكمية | السعر
-------------------|--------|-------
حزمة المراقبة الكاملة | 1   | 1500
```

---

## ⚠️ ملاحظات مهمة:

1. **البيانات القديمة آمنة**
   - جميع القطع الموجودة ستبقى كما هي
   - الأعمدة الجديدة اختيارية (nullable)

2. **لا يؤثر على الوظائف الأخرى**
   - التسليم والصرف والإتلاف تعمل كما هي
   - التقارير الأخرى لن تتأثر

3. **يمكن التراجع**
   - إذا أردت إزالة الأعمدة لاحقاً:
   ```sql
   ALTER TABLE inventory_items DROP COLUMN bundle_group_id;
   ALTER TABLE inventory_items DROP COLUMN bundle_name;
   ```

---

## 🆘 في حالة المشاكل:

إذا واجهت أي مشكلة:
1. تأكد من أنك في المشروع الصحيح
2. تأكد من أن لديك صلاحيات Admin
3. جرب تشغيل كل أمر على حدة
4. تحقق من رسائل الخطأ

---

## ✅ بعد التنفيذ:

يمكنك الآن:
1. استلام حزم كاملة من صفحة الاستلام
2. عرض المطالبات المالية بشكل صحيح
3. رؤية الحزم كوحدة واحدة في التقارير
