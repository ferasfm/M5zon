# دليل البناء السريع ⚡

## 🚀 خطوات البناء في 5 دقائق

### 1️⃣ التحضير
```bash
# تأكد من تثبيت المكتبات
npm install
```

### 2️⃣ إعداد قاعدة البيانات
```bash
# تشغيل سكريبت الإعداد
node scripts/setup-auth-system.js
```

### 3️⃣ الاختبار
```bash
# اختبار في المتصفح
npm run dev

# أو اختبار في Electron
npm run dev:electron
```

### 4️⃣ البناء
```bash
# بناء ملفات الويب
npm run build

# بناء تطبيق سطح المكتب
npm run build:electron
```

### 5️⃣ العثور على الملفات
```
📁 dist/
  ├── 📦 m5zon Setup 1.0.0.exe      ← ملف التثبيت
  ├── 📦 m5zon 1.0.0.exe            ← النسخة المحمولة
  └── 📁 win-unpacked/              ← ملفات التطبيق
```

## ⚡ أوامر سريعة

### البناء الكامل (كل شيء)
```bash
npm run build && npm run build:electron
```

### بناء Installer فقط
```bash
npm run build && npm run build:installer
```

### بناء Portable فقط
```bash
npm run build && npm run build:portable
```

## 🎯 الأوامر المتاحة

| الأمر | الوصف |
|-------|-------|
| `npm run dev` | تشغيل في المتصفح |
| `npm run dev:electron` | تشغيل في Electron |
| `npm run build` | بناء ملفات الويب |
| `npm run build:electron` | بناء تطبيق سطح المكتب |
| `npm run build:installer` | بناء Installer فقط |
| `npm run build:portable` | بناء Portable فقط |

## 🔧 حل المشاكل السريع

### المشكلة: خطأ في البناء
```bash
# الحل
rmdir /s /q dist node_modules
npm install
npm run build:electron
```

### المشكلة: التطبيق لا يعمل
```bash
# الحل
npm run build
npm run build:electron
```

### المشكلة: الأيقونة لا تظهر
```bash
# تأكد من وجود icon.ico في public/
# ثم أعد البناء
npm run build:electron
```

## 📊 الحجم المتوقع

- **Installer**: ~150-200 MB
- **Portable**: ~150-200 MB
- **مجلد dist**: ~300-400 MB

## ✅ قائمة التحقق

- [ ] تثبيت المكتبات (`npm install`)
- [ ] إعداد قاعدة البيانات (`node scripts/setup-auth-system.js`)
- [ ] اختبار التطبيق (`npm run dev`)
- [ ] بناء الويب (`npm run build`)
- [ ] بناء Electron (`npm run build:electron`)
- [ ] اختبار الملف المبني
- [ ] توزيع التطبيق

## 🎉 تم!

الآن لديك تطبيق سطح مكتب جاهز في مجلد `dist`! 🚀

---

**للمزيد من التفاصيل، راجع:** `BUILD_DESKTOP_APP.md`
